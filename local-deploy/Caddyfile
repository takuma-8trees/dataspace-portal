:80 {
    # Route API calls to oauth2-proxy which forwards to backend (requires oauth2-proxy client setup)
    route /api/* {
        reverse_proxy oauth2-proxy:8080
    }

    # All other traffic goes to the frontend
    reverse_proxy frontend:8080
}
